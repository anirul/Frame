#pragma once

#if !defined(FRAME_INTERNAL_BACKEND_ACCESS)
#error "frame/opengl and frame/vulkan are internal backend APIs and out of band for user code. Use frame/api.h and Frame interface types instead."
#endif
